[pytest]
norecursedirs = dz_211025
markers =
    tests: unites all tests
    average: тести для функції average
    adults: тести для функції adults
    is_even: checks if number is even
    max_value: determines max value from dict
    validate_password: determines if password is valid
    is_valid_email:determines if email is valid
    is_prime:determines if number is primal